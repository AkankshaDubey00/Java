import java.util.*;
public class arrayListExample {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        //syntax
        ArrayList<Integer> list = new ArrayList<Integer>(10);
        // list.add(29);
        // list.add(30);
        // list.add(31);
        // list.add(32);
        // list.add(33);
        // list.add(34);
        // list.add(325);
        // list.add(36);
        // list.add(37);
        // System.out.println(list.contains(32));  to check the number is in the list or not .
        // list.set(0, 99); // set new elemment in the list 
        // list.remove(2);
        // System.out.println(list);
        

        // input 
        for (int i = 0; i < 5; i++) {
            list.add(sc.nextInt());
        }

        //get item at any index

        // input 
        for (int i = 0; i < 5; i++) {
           System.out.println(list.get(i)); // pass index here , list(indes ) syntax will not work here 
        }

        // System.out.println(list);
        
    }
}
